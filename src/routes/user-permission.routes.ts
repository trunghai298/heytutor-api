import { Application } from "express";
import UserPermissionController from "../controller/user-permission.controller"

export default (app: Application) => {
    app
    // .post("/init-permission", UserPermissionController.initPermission)
}